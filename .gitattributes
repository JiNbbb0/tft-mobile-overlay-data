# Repository-authored text remains normalized and portable.
*.md text eol=lf
*.ps1 text eol=lf
*.yml text eol=lf
*.yaml text eol=lf

# Published bytes are content-addressed and must never be changed by Git EOL conversion.
# These rules intentionally come last so they override the generic Markdown rule above.
site/** -text
source/** -text
