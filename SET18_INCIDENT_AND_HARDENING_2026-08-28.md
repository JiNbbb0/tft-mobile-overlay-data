# Set 18 障害レポートと恒久対策

日付: 2026-08-28  
対象: TFT Mobile Overlay データ更新基盤  
対象セット: TFTSet18 / Patch 18.1 / Cluster 422

## 1. 事象

Set 18 への切り替え後、図鑑のユニット・特性・アイテム・オーグメントは新セットへ更新された一方、構成一覧が 0 件になった。

調査時点では MetaTFT の構成統計そのものは存在し、Platinum+ 条件で 42 構成が候補条件を満たしていた。しかし、構成別おすすめオーグメントなど一部の付随情報が遅れていたため、生成処理が利用可能な構成本体まで除外していた。

最終的に Set 18 の構成 18 件を公開し、公開 HTTPS 検証まで成功した。

## 2. 今回発生した問題と原因

### 2.1 CommunityDragon の新セットデータが部分的だった

**症状:** 通常の派生データから取得できたプレイ可能ユニットが 2 体しかなかった。

**簡単な原因:** 新セット開始直後は、公開元ごとにデータが揃う時間が違う。CommunityDragon の高水準 JSON が先に完全になるとは限らなかった。

**対処:** raw client data から現行セットのユニットを再構築するフォールバックを使用し、Set 18 では 74 体を復元した。

### 2.2 空配列が JSON null / 非配列として扱われる危険があった

**症状:** Android 側が `getJSONArray()` で読む項目に null が入ると、更新データ全体を正常に扱えない可能性があった。

**簡単な原因:** PowerShell の空コレクションは、書き方によって `[]` ではなく null 相当へ変換されることがある。

**対処:** `augments` と `compositions` は必ず JSON 配列として出力する契約に変更した。

### 2.3 構成別オーグメントが無いだけで構成本体まで消えていた

**症状:** 構成一覧が 0 件。

**簡単な原因:** 「おすすめオーグメントが取得できること」を構成を採用する必須条件にしていた。Set 18 では構成統計が先に到着し、構成別オーグメントが後から到着したため、18 構成すべてが落とされた。

**対処:** 新セット準備中は、オーグメントが未到着でも表示可能な構成は残す。おすすめオーグメントだけ空配列として扱う。

### 2.4 準備中データを完成済みと判定する可能性があった

**症状:** 構成数だけ揃えば `META_STABLE` と見なせる設計だった。

**簡単な原因:** 「18 構成あるか」だけでは、付随メタ情報が完成しているか判定できない。

**対処:** 構成別おすすめ情報などが欠ける間は `META_COLLECTING` を維持する。

### 2.5 検証器が部分準備状態を理解していなかった

**症状:** 構成を残すよう直した後、検証器が「おすすめオーグメントが 0 件」という理由で公開を止めた。

**簡単な原因:** 生成側だけ部分データを許可し、検証側は従来の完成データ前提のままだった。

**対処:** `META_COLLECTING` の間だけ、オプション情報の欠落を許容する。`META_STABLE` では従来通り必須にする。

### 2.6 データ提供元どうしでアイテム ID が一致しなかった

**症状:** `DA_ArchangelsStaff` と `TFT_Item_ArchangelsStaff` が同じアイテムなのに別 ID と見なされ、検証で停止した。

**簡単な原因:** MetaTFT、CommunityDragon、ローカルカタログが必ず同じ ID 名前空間を使うとは限らない。

**対処:** ID の既知プレフィックスを正規化し、正式カタログ ID へ変換する。必要時は名称でも照合するが、候補が複数なら推測せず失敗させる。

### 2.7 テストの実行順が実運用とずれていた

**症状:** 互換修正を適用する前に新セットテストが走っていたため、実際に生成されるコードとテスト対象が完全には一致していなかった。

**簡単な原因:** 「互換パッチ適用」と「回帰テスト」の順序が逆だった。

**対処:** 互換修正を先に適用し、その後に回帰テストと互換契約検証を行う。

### 2.8 公開データは正常でもユーザーには「0件」にしか見えなかった

**症状:** 図鑑が使えるのに構成だけ未準備、という状態をアプリが説明できなかった。

**簡単な原因:** 機械向け `readiness` は存在したが、機能単位の状態・ユーザー向け文言を固定 URL で配信していなかった。

**対処:** `data-quality.json` を追加し、カタログ・構成・構成別オーグメントを別々に `READY / PARTIAL / COLLECTING` として表現する。日本語・英語のユーザー向けメッセージも配信する。

## 3. 現時点で想定できる将来の障害

1. 新セットで CommunityDragon のユニット、特性、名称、画像が遅れる。
2. MetaTFT の cluster 情報と comps_data / comps_stats が一時的に別セットを指す。
3. 構成統計はあるが comp_details、positioning、level board、item build、augment tier の一部だけ遅れる。
4. 新パッチ直後にサンプル数が少なく、Platinum+ だけでは必要構成数に届かない。
5. `DA_*`、`TFT_Item_*`、`TFTxx_Item_*` など ID プレフィックスが変わる。
6. 同じ短縮 ID に複数候補が存在し、単純な文字列置換では誤マッピングする。
7. JSON の空コレクションが null、object、scalar へ崩れる。
8. 画像 URL が未設定、404、壊れた PNG、別セット画像になる。
9. Riot のパッチノート HTML / URL / ローカライズ表現が変わり、パッチ番号検出が失敗する。
10. B パッチでセット・cluster ID は同じまま、内容だけ変化する。
11. MetaTFT / CommunityDragon の一時障害、429、5xx、タイムアウトが発生する。
12. robots.txt や利用条件が変化し、取得を継続すべきでなくなる。
13. GitHub Actions は成功しても Pages が古い manifest を返す、または CDN 反映が遅れる。
14. `data-index.json` と manifest は一致していても、固定ルートの状態ファイルだけ古い。
15. 検証が厳しすぎて利用可能な主要機能まで止める、または緩すぎて壊れたデータを公開する。
16. 新セット用の一時フォールバックが、その後の通常更新でも残り続ける。
17. データは正しいが、アプリ側が新しい readiness / quality 状態を無視して「0件」とだけ表示する。
18. GitHub Pages 容量・履歴保持・大量画像により配信サイズが増えすぎる。

## 4. 恒久対策の設計原則

### 4.1 「必須データ」と「後から来てもよいデータ」を分離する

ユニット一覧や構成本体のように利用可能なら見せるべき情報と、構成別おすすめオーグメントのような付加情報を同じ必須条件にしない。

### 4.2 失敗は機能単位で縮退させる

新セットの図鑑が完成しているなら図鑑を止めない。構成が利用可能なら、付加おすすめ情報が遅れていても構成を止めない。作れない情報を捏造せず `COLLECTING` と明示する。

### 4.3 ID は提供元の文字列を直接信用しない

公開前に canonical catalog ID へ統一する。完全一致、既知プレフィックス差、名称一致の順で解決し、曖昧なら公開を止める。

### 4.4 「生成できた」ではなく「ユーザーが読める」ことを検証する

JSON 型、参照先 ID、画像、構成数、準備状態、公開 HTTPS までを別々に検証する。

### 4.5 新セット直後を通常時と同じ完成条件にしない

`CATALOG_READY -> META_COLLECTING -> META_STABLE` の段階を維持し、途中状態を正式な状態として扱う。

### 4.6 品質状態を固定 URL で公開する

`data-quality.json` を機械・アプリ・監視共通の状態契約とする。アプリは将来的にこのファイルを使って「準備中」「一部情報収集中」を表示できる。

### 4.7 自動化自身を監視する

構成が長時間 0 件のまま、品質状態が latestVersionId と不一致、固定状態ファイルが公開されていない、といった状態を watchdog が検出できるようにする。

## 5. 2026-08-28 に追加した再発防止

- `id-compatibility-policy.ps1`: 提供元をまたぐ ID 正規化を汎用化。
- `test-id-compatibility-policy.ps1`: `DA_*`、`TFT_Item_*`、将来の `TFT19_Item_*` などを合成テスト。
- `verify-runtime-hardening.ps1`: 実際の生成コードに互換対策が適用済みかを生成前に確認。
- `validate-data-compatibility.ps1`: JSON 配列契約、canonical ID、セット整合、readiness と構成品質を生成後に再検証。
- `write-data-quality-status.ps1`: 固定 URL の `data-quality.json` を生成。
- `data-quality.schema.json`: 品質状態 API のスキーマを固定。
- `check-public-data-quality.ps1`: 公開品質状態を watchdog から確認。
- refresh workflow: 互換修正を先に適用し、その後に回帰テスト・互換テスト・生成後検証を行う順序へ変更。
- publication reconciliation: `data-quality.json` も公開同期対象に加え、manifest だけ一致して状態ファイルが古いケースを検知する。
- watchdog: `CATALOG_ONLY` が長時間続く、品質状態が公開版と不一致、状態ファイルが取得不能な場合に自動化ヘルス問題として扱う。

## 6. 残る制約

データ配信側は、部分データを安全に配信し、何が準備中かを機械可読で説明できるところまで強化した。

一方、Android アプリ本体のリポジトリはこの GitHub 接続から参照できない。そのため、`data-quality.json` の `userMessageJa` を画面に表示して「0件」ではなく「構成データを収集中」と案内する UI 変更は、このデータリポジトリだけでは実装できない。アプリ側では次回 APK 更新時にこの固定状態 API を読む実装を追加することが望ましい。
